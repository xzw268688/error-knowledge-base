# 错误知识库 Skill 需求文档

## 1. 概述

设计一个 Claude Code Skill，用于管理个人/项目的错误解决经验。每次解决错误后自动建议记录，下次遇到问题时自动检索知识库，避免重复踩坑。

## 2. 核心功能

### 2.1 错误检索
- **触发时机**：用户在对话中描述错误或贴出错误信息时自动触发
- **检索范围**：`.claude/knowledge-base/` 目录下所有 markdown 文件
- **匹配策略**：语义理解为主 + 结构化字段（tags、error_type、files）辅助过滤
- **返回结果**：
  - 命中：返回相似错误的解决步骤
  - 未命中：告知"知识库中无相似错误记录"

### 2.2 错误记录
- **触发时机**：在对话中自动识别"错误已解决"信号（如"解决了"、"搞定了"、问题已修复等），主动询问用户"是否记录此错误？"
- **用户确认后**：自动生成结构化 markdown 文档并写入知识库
- **文件命名**：`YYYY-MM-DD-error-slug.md`（日期 + 错误简短描述）
- **文档结构**：
  ```yaml
  ---
  title: 错误标题
  date: YYYY-MM-DD
  tags: [标签1, 标签2]
  error_type: 错误类型/错误码
  files: [相关文件路径]
  ---

  # 错误现象
  ...

  # 解决步骤
  ...

  # 根因分析（可选）
  ...
  ```

## 3. 设计决策

| 需求点 | 方案 | 理由 |
|--------|------|------|
| 相似性判断 | 语义理解 + 结构化字段辅助 | 覆盖模糊匹配（"OOM" vs "内存溢出"） |
| 记录触发 | 自动识别"错误已解决"，主动询问 | 比纯手动更主动，比全自动更可控 |
| 检索触发 | 自动检测错误类型并检索 | 符合"下次遇到问题先调用 skill"需求 |
| 存储组织 | 一个 markdown 文件一条记录 | 结构清晰，独立管理 |
| 存储位置 | 当前项目 `.claude/` 下 | 跟随项目，团队可共享 |

## 4. 目录结构

```
项目根目录/
├── .claude/
│   ├── knowledge-base/           # 错误知识库
│   │   ├── 2026-09-05-npm-eresolve.md
│   │   ├── 2026-09-03-python-oom.md
│   │   └── ...
│   └── skills/
│       └── error-knowledge-base/
│           └── SKILL.md          # skill 主文件
```

## 5. 使用流程

### 5.1 遇到问题时
1. 用户描述错误（如"npm install 报 ERESOLVE 错误"）
2. Skill 自动读取 `.claude/knowledge-base/`
3. 语义匹配已有记录
4. 返回相似错误及解决方案，或"无相似记录"

### 5.2 解决问题后
1. Skill 检测到对话中出现错误已解决的信号
2. 主动询问："是否将此错误及解决方案记录到知识库？"
3. 用户确认
4. Skill 自动整理错误信息 + 解决方案
5. 生成 markdown 文件并写入知识库

## 6. 验收标准

- [ ] 用户描述错误时，skill 能自动检索知识库
- [ ] 语义匹配能找到表述不同但本质相似的错误
- [ ] 结构化字段（tags、error_type）能正确辅助过滤
- [ ] 错误解决后能自动识别并询问记录
- [ ] 生成的 markdown 文件符合预定结构
- [ ] 知识库文件位置正确（项目根目录的 `.claude/knowledge-base/`）
